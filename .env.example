# Kigali EMS Pipeline Optimization — local environment variables
# Copy this file to ".env" and fill in values for your machine.
# NOTE: ".env" is ignored by git.

# Path to your SUMO installation (required for TraCI/sumolib tools).
# Example (macOS, Homebrew): /opt/homebrew/opt/sumo/share/sumo
SUMO_HOME=

# Optional: project root path (helps some notebooks/tools resolve paths)
PROJECT_ROOT=

# Where long-running outputs and checkpoints are stored
RUNS_DIR=runs

# Data directory root
DATA_DIR=data

# Optional reproducibility hint (keeps hashing stable across runs)
PYTHONHASHSEED=0
